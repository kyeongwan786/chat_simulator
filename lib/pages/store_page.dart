import 'package:flutter/material.dart';

class StorePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("스토어")),
      body: Center(
        child: Text("프리미엄/티켓 구매 (Dummy)"),
      ),
    );
  }
}
